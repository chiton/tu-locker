## 1. Logo Component

- [x] 1.1 Create a reusable `Logo` component with an inline SVG locker + padlock mark.
- [x] 1.2 Support normal brand lockup usage with mark, `tu-locker` wordmark, and `self-storage` descriptor.
- [x] 1.3 Support compact/icon-only usage for small contexts.
- [x] 1.4 Support light and dark surface variants without duplicating components.

## 2. Site Integration

- [x] 2.1 Replace the current `TL` badge in the header with the new logo component.
- [x] 2.2 Update footer branding to use the same logo identity.
- [x] 2.3 Add or update site icon metadata with a simplified locker + padlock mark when practical.

## 3. Visual Quality

- [x] 3.1 Verify the logo remains legible on mobile, tablet, and desktop header layouts.
- [x] 3.2 Verify contrast and spacing on white header and dark footer backgrounds.
- [x] 3.3 Ensure the new logo does not introduce external dependencies or remote assets.

## 4. Verification

- [x] 4.1 Run linting and fix reported issues.
- [x] 4.2 Run a production build and fix build errors.
- [x] 4.3 Review implementation against the `Brand Logo Identity`, `Logo Consistency Across Site`, and `Logo Visual Fit` requirements.
